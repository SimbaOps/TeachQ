package com.bignerdranch.android.clickerquestions.structs;

//This ENUM creates two possible types for a school member
//A teacher and a student, if we ever needed to add more we could,
//Also it forces people to write TEACHER or STUDENT rather than true or false
//When choosing between teacher or student.
public enum SchoolType {
    STUDENT, TEACHER
}
